import java.util.Arrays;
import java.util.Collections;
import java.util.Comparator;

public class uts {
    public static void main(String[] args) {
        Integer[] nomor = {3,6,1,9};
        Arrays.sort(nomor, Collections.reverseOrder());
        System.out.print(Arrays.toString(nomor));
        System.out.println((double)1.0/3.0);
    }
    public static int Tebak(int x, int y) {
        char a = 'a';
        if(x == 0) return y;

        r.

        else return Tebak(x-1,y)+1;

    }
}
